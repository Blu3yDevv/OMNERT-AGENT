# Minimal, effective configuration
model: "llama-3.3-70b-versatile"
search:
  provider: "tavily"
  max_results: 3
  timeout: 30
memory:
  max_history: 10
  enable_summarization: false
logging:
  level: "INFO"
  file: "agent.log"